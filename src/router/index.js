import { createRouter, createWebHistory } from 'vue-router';

const routes = [
  // ที่นี่คุณสามารถกำหนดเส้นทางของคุณ
];

const router = createRouter({
  history: createWebHistory(),
  routes,
});

export default router;
